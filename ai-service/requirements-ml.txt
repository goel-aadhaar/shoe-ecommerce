# --- Heavy ML / embedding deps (pulled in the Docker image; optional for local core dev) ---
# Installed on top of requirements.txt. Kept separate so the core service and the
# test suite stay importable without downloading torch.

numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
scipy==1.13.1

# Embeddings (Phase 2)
sentence-transformers==3.3.1

# Collaborative filtering (Phase 3)
lightfm==1.17
implicit==0.7.2
scikit-surprise==1.1.4

# Association-rule mining / bundles (Phase 3)
mlxtend==0.23.3
