import { createContext } from 'react';

// Create and export the AuthContext object.
// This file is now purely for the context,
// which prevents the Fast Refresh error.
export const AuthContext = createContext();
